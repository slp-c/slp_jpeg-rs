# Baseline Sequential JPEG codec written in Rust

- This is just a learning project
- For now I only have the decoder working

- Read src/lib.rs at impl Image { pub fn read_jpeg_from_file(path: &str) .. } to see how the API look like
